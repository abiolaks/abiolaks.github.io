# Kingsley Lawani Portfolio

Static portfolio site (HTML/CSS/JS), styled after glifo.cat. See `CONTEXT.md`
for the domain model and conventions.

## Agent skills

### Issue tracker

GitHub Issues on the portfolio repo (interim: local markdown under `.scratch/`).
See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
